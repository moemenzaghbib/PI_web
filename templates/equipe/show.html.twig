

{% block body %}

    <table class="table" style="margin-left: 500px;margin-top: 200px;width: 800px">
        <tbody>
        <tr>
            <th>ID Equipe</th>
            <td>{{ equipe.idEquipe }}</td>
        </tr>
        <tr>
            <th>Numero Equipe</th>
            <td>{{ equipe.numEquipe }}</td>
        </tr>
        <tr>
            <th>Nombre d'Employee</th>
            <td>{{ equipe.nbreEmp }}</td>
        </tr>
        <tr>
            <th>Nom Service Equipe</th>
            <th>{{ equipe.service }}</th>
        </tr>
        </tbody>
    </table>
    <div style="margin-left: 800px"> <a href="{{ path('app_equipe_index')  }}"  class="btn btn-primary">back to list</a>

        <a href="{{ path('app_equipe_edit', {'idEquipe': equipe.idEquipe}) }}" class="btn btn-primary">edit</a>

    </div>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
